import { LightningElement } from 'lwc';
import errorsymbol from '@salesforce/resourceUrl/errorsymbol';

export default class MddCrisisResource extends LightningElement {

    errorsymbol = errorsymbol;
}
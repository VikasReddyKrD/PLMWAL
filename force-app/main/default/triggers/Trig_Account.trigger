trigger Trig_Account on Account (after insert, after update) {
    //if(!Test.isRunningTest()) {
       //et4ae5.triggerUtility.automate('Account');  ///sreehari commented this for time being until Asheer finds fix for marketing cloud integration
    //}  
    //else {
      //  List<Account> accts = new List<Account>();
       // for(Account acct : Trigger.new) {
        //    accts.add(acct);
        //}
    //}
}
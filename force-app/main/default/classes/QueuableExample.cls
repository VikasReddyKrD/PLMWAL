public class QueuableExample implements Queueable{
    public void execute(QueueableContext context){
        Account acc=new Account(Name='king');
        Insert acc;
        System.debug('Q1');
    }
}